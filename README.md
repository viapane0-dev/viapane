
  # High-Fidelity Website Layout

  This is a code bundle for High-Fidelity Website Layout. The original project is available at https://www.figma.com/design/qYtIDLJ18NYsSsAllNadtg/High-Fidelity-Website-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  